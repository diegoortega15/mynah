# Screenshots do README

Coloque aqui os prints do app rodando, com **exatamente** estes nomes (o `README.md` da raiz já os referencia):

| Arquivo | Tela | Como capturar |
|---|---|---|
| `dashboard.png` | 🏠 Início | Abra `/` — mostre o cabeçalho (dia/fase/streak) e os 4 blocos. |
| `vocab.png` | 🗂️ Vocabulário | Abra `/vocab` — de preferência com uma sessão de revisão (flashcard) aberta. |
| `listening.png` | 🎧 Ouvir | Abra `/listening` com um diálogo gerado (aba "Diálogo (IA)"). |
| `speaking.png` | 🗣️ Fala | Abra `/speaking` no Shadowing (com a nota) ou no Tutor. |

Dicas:

- Largura ~1200–1400px, tema escuro (o padrão) fica ótimo.
- PNG. Se quiser reduzir o tamanho, exporte a ~1x/2x sem exagerar.
- Opcional: um `demo.gif` curto (10–15s) navegando pelos blocos deixa o README ainda melhor — se criar, adicione uma linha no topo do README.
